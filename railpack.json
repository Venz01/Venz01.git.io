{
  "php": "8.4",
  "build": {
    "install": [
      "composer install --no-dev --optimize-autoloader",
      "npm ci",
      "npm run build"
    ],
    "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
